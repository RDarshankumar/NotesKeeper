<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="View details of a specific file in a dark-themed card layout."
    />
    <title>Dark Themed Card</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 text-white">
    <div
      class="max-w-lg mx-auto bg-gray-800 text-white rounded-lg p-6 shadow-lg mt-6"
    >
      <h3 class="text-2xl font-bold mb-4 text-center">Edit Name</h3>

      <!-- Form for editing name -->
      <form action="/edit" method="post">
        <!-- Previous Name Input (readonly) -->
        <div class="mb-6">
          <label for="previous-name" class="block text-sm font-medium mb-2"
            >Previous Name</label
          >
          <input
            type="text"
            name="previous"
            value="<%= filename %>" 
            class="w-full p-3 bg-gray-700 text-white rounded-md shadow-md focus:outline-none focus:ring-2 focus:ring-blue-500"
            placeholder="Previous Name"
            readonly
          />
        </div>

        <!-- New Name Input -->
        <div class="mb-6">
          <label for="new-name" class="block text-sm font-medium mb-2"
            >New Name</label
          >
          <input
            type="text"
            name="new"
            class="w-full p-3 bg-gray-700 text-white rounded-md shadow-md focus:outline-none focus:ring-2 focus:ring-blue-500"
            placeholder="New Name"
            required
          />
        </div>

        <!-- Submit Button -->
        <div class="mb-6">
          <button
            type="submit"
            value="UpdateName"
            class="w-full p-3 bg-yellow-600 text-white rounded-md shadow-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500"
          >
            Update Name
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
